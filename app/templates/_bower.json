{
	"name": "<%= _.slugify(appName) %>",
	"version": "0.0.0",
	"dependencies": {
<% if (includeCompass) { %>
		"bower-compass-core": "~0.0.1",
<% } %>

<% if (includeRequireJS) { %>
		"requirejs"		: "~2.1.8",
		"amd-modules"	: "is-real/amd-modules",
<% } %>

<% if (includeModernizr) { %>
		"modernizr"		: "~2.6.2",
<% } %>

		"jquery"		: "~1.9.1"
	},
	"devDependencies": {}
}
